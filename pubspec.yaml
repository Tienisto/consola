name: consola
description: A utility library to help developing command-line applications in Dart. Provides screen manipulation, ANSI escape codes, and more.
version: 0.1.0
repository: https://github.com/Tienisto/consola
topics:
  - console
  - ansi
  - cli
  - terminal

environment:
  sdk: ^3.0.0

dependencies:
  meta: ^1.9.1

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
